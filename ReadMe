Building the image

docker build -t personnel .


running the image

docker run -d -p 8080:80 --name MyWebsite personnel


open this link

https://madanreddy123.github.io/NewWebsiteShowingTheProfessionalDetails/


import java.io.BufferedReader;
import java.io.FileReader;
import java.io.IOException;
import java.util.HashMap;
import java.util.Map;

public class ConfigUtility {

    private Map<String, String> keyValueMap = new HashMap<>();

    // Constructor that loads the configuration from the file
    public ConfigUtility(String filePath) {
        loadConfig(filePath);
    }

    // Method to load key-value pairs from the file
    private void loadConfig(String filePath) {
        try (BufferedReader br = new BufferedReader(new FileReader(filePath))) {
            String line;
            while ((line = br.readLine()) != null) {
                String[] parts = line.split("=", 2);
                if (parts.length >= 2) {
                    String key = parts[0].trim();
                    String value = parts[1].trim();
                    keyValueMap.put(key, value);
                } else {
                    System.out.println("Ignoring malformed line: " + line);
                }
            }
        } catch (IOException e) {
            e.printStackTrace();
        }
    }

    // Method to get the value assigned to a specific key
    public String getValue(String key) {
        return keyValueMap.get(key);
    }

    // Main method for testing purposes
    public static void main(String[] args) {
        ConfigUtility configUtility = new ConfigUtility("config.txt");
        
        // Test retrieving values for specific keys
        String value1 = configUtility.getValue("key1");
        System.out.println("Value for key1: " + value1);
        
        String value2 = configUtility.getValue("key2");
        System.out.println("Value for key2: " + value2);
        
        String value3 = configUtility.getValue("key3");
        System.out.println("Value for key3: " + value3);
    }
}
